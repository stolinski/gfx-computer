import type { BlockPipelineDefinition } from '$lib/platform/pipelines/definition-types';

export const kineticWordBlockDefinition = {
	type: 'kinetic-word'
} satisfies BlockPipelineDefinition<'kinetic-word'>;
